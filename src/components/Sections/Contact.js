import * as React from "react"
import PropTypes from "prop-types"
import { Icon } from '@iconify/react';

const Contact = ({  }) => (
    <article id='contact' className={`mainContent container mx-auto min-h-screen`} >
        Contact Section
    </article>
)

export default Contact
